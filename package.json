{
  "name": "Store-Games",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "asb",
    "test": "asp --nologo --verbose",
    "clean": "rm -rf ./build",
    "deploy": "near dev-deploy ./build/release/Store-Games.wasm"
  },
  "dependencies": {
    "near-sdk-as": "3.1.0"
  },
  "devDependencies": {
    "shelljs": "^0.8.4"
  }
}
