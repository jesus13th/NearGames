{
  "name": "StoreGames",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "asb",
    "test": "asp --nologo --verbose",
    "clean": "rm -rf ./build",
    "deploy": "near dev-deploy ./build/release/Store-Games.wasm"
  },
  "author": "Jesus Robles",
  "license": "ISC",
  "dependencies": {
    "near-sdk-as": "3.1.0"
  },
  "devDependencies": {
    "shelljs": "^0.8.4"
  }
}
